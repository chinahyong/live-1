<?xml version="1.0" encoding="utf-8"?>
<com.bixin.bixin.ui.LoadingProgress xmlns:android="http://schemas.android.com/apk/res/android"
                               xmlns:app="http://schemas.android.com/apk/res-auto"
                               android:id="@+id/progress"
                               style="@style/Base.MM"
                               android:gravity="center"
                               android:orientation="vertical"
                               app:LoadingImage="@+id/loading_showimage"
                               app:LoadingInfo="@+id/loading_info"
                               app:LoadingProgress="@+id/loading_progress">

    <ImageView
        android:id="@+id/loading_showimage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:src="@drawable/a_common_no_data"
        android:visibility="gone"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="100dip"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/loading_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:src="@drawable/logoing"/>

        <TextView
            android:id="@+id/loading_info"
            style="@style/Base.WW"
            android:gravity="center_horizontal"
            android:lineSpacingMultiplier="1.4"
            android:padding="10dp"
            android:text="@string/a_loading_text"
            android:textColor="@color/event_item_expire_text"
            android:textSize="14sp"/>
    </LinearLayout>

</com.bixin.bixin.ui.LoadingProgress>